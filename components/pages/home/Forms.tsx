import {FC} from "react";import {Box, Button, Container, Theme, Typography} from "@mui/material";import {makeStyles} from "@mui/styles";import {media} from "../../../utility/media";import {howToUseSteps} from "../../../constants/main";import { useForm } from "react-hook-form";import api from "../../../http/api";const containerPY = media(30, 45);const useStyles = makeStyles((theme: Theme) => ({    smartAnimate: {        padding: ` 5px ${media(20, 50)}`,        background: "white",        boxShadow: "0px 4px 4px rgba(0, 0, 0, 0.25)",        borderRadius: '8px',        border: "none",        color: "black",        fontFamily: 'Raleway',        fontStyle: "normal",        fontWeight: '400',        lineHeight: "23px",        letterSpacing: "0.2em",        "&:hover": {            background: "rgba(255,255,255,0.73)",        }    },    form_input: {        maxWidth: media(400, 470),        width: "100%",        boxSizing: "border-box",        paddingLeft: "10px",        height: '40px',        background: "rgba(94, 94, 94, 0.41)",        margin: "10px 2px",        border: 'none',        color: "white",        borderRadius: "5px",        fontStyle: "normal",        fontWeight :"400",        fonsSize: "px",        lineHeight: "20px",    },    textaria_input: {        maxWidth: media(400, 860),        width: "100%",        paddingLeft: "10px",        background: "rgba(94, 94, 94, 0.41)",        resize: "vertical",        height: "80px",        color: "white",        marginTop: "15px",        border: 'none',        borderRadius: "5px"    },    saveBtn: {        padding: "5px 40px",        background: "#287AF4",        "&:hover": {            background: "#287AF4"        },    },    contactTitle: {        fontFamily: 'sans-serif',        fontStyle: "normal",        fontWeight: "400",        fontSize: "24px",        lineHeight: "29px",        textAlign: "center",        letterSpacing: "0.2em",        color:" #FFFFFF"    }}));const Forms: FC = () => {    const styles = useStyles();    const { register, handleSubmit, watch, formState: { errors } } = useForm();    const onSubmit = (data:any) => {        api.post("/users/send-mail-message/", data)            .then(() => {                alert("success")            })    };    console.log(watch("example"));    return (        <div style={{background: "#181818", padding: `${media(10, 40)} 20px`}}>            <Container maxWidth="lg">                <Box style={{textAlign: "center"}}>                    <h1 className={styles.contactTitle}>CONTACTS</h1>                </Box>                <form onSubmit={handleSubmit(onSubmit)}>                    <Box style={{textAlign: "center", marginTop: "50px"}}>                        <input type="text" className={styles.form_input} {...register("first_name")} placeholder="First name"/>                        <input type="text" className={styles.form_input} {...register("last_name")} placeholder="Last name"/>                        <input type="text" className={styles.form_input} {...register("mobile")} placeholder="Mobile"/>                        <input type="email" className={styles.form_input} {...register("email")} placeholder="Email"/>                    </Box>                    <Box style={{textAlign: "center"}}>                        <textarea className={styles.textaria_input} {...register("message")} placeholder="Text"/>                    </Box>                    <Box style={{textAlign: 'center', marginTop: "30px"}}>                        <Button className={styles.smartAnimate} type="submit">                            SEND                        </Button>                    </Box>                </form>            </Container>        </div>    )}export default Forms;